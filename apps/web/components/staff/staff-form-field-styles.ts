import { drawerScrollAreaClassName } from "@/lib/ui/drawer-form-section";

/** Einheitliche Felder in Mitarbeiter-Bottom-Sheets (Höhe wie Rest der App). */
export const staffDrawerFieldClassName =
  "h-11 w-full min-w-0 rounded-xl";

/** Scroll-Bereich Mitarbeiter-Sheets (Padding 5). */
export const staffDrawerScrollClassName = drawerScrollAreaClassName(5);
