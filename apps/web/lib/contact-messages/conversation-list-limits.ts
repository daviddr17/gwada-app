/** Max. DB-Zeilen pro Plattform für Konversationsliste (neueste zuerst). */
export const CONVERSATION_LIST_MESSAGE_ROW_LIMIT = 2_500;

/**
 * Dashboard-/Glocken-Light-Pfad: Unread-Preview braucht keine volle Inbox-Historie.
 * Neueste zuerst — reicht für Unread-Zähler und Widget-Vorschau.
 */
export const DASHBOARD_CONVERSATION_LIST_MESSAGE_ROW_LIMIT = 400;
