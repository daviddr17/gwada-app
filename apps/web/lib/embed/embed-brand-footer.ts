/** Öffentliches Logo für Embed-Footer (iframe + gwada.js). */
export const EMBED_BRAND_FOOTER_LOGO_PATH = "/api/platform/favicon";

/** Ca. Höhe des Footers — Host-Seiten können Mindesthöhe optional erhöhen. */
export const EMBED_BRAND_FOOTER_HEIGHT_PX = 36;
